#include <stdio.h>\

int main(int argc, char* argv[]) {
    
    //want name, age as command-line args
    //print error if arguments are incorrect

    char* name = argv[0];

    //can we do this?
    int age = argv[1];

    //now print name, age

    return 0;
}